---
description: Show current aoe-bus peer links for this session
allowed-tools: Bash(aoe-bus:*)
---

Show which peer sessions this one is linked to.

!`aoe-bus links`

If there are no links, suggest `/bus-link <peer-label>` to add one. If there are links, the user knows them now — no extra commentary needed unless they ask.
