---
description: Roll-up of bus state across all aoe sessions — who needs your attention
allowed-tools: Bash(agora:*)
---

Print a table showing every aoe session's bus state: link count, unread inbox bytes, owed replies, awaited replies, escalations. Sessions needing attention are bulleted with ●.

Run:

!`agora status`

Don't summarize the output — the table is the answer. Just confirm it ran.
