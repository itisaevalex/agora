---
description: Show the lineage tree — your ancestors, you, your descendants, plus non-lineage peers
allowed-tools: Bash(agora:*)
---

Print this session's place in the agora lineage tree — ancestors above (oldest first), descendants below (recursive), and any non-lineage linked peers at the bottom.

!`agora tree`

If the output is empty, this session has no spawned children and no parent (it's a top-level session).
