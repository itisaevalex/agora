---
description: Unlink this session from a peer
argument-hint: <peer-label-or-aoe-id-prefix>
allowed-tools: Bash(agora:*)
---

The user wants to remove a peer link from this session.

!`agora unlink "$ARGUMENTS"`

Report the result. If `· no link matching`, mention the user can run `/agora-links` to see what's currently linked.
