---
description: Show current agora peer links for this session
allowed-tools: Bash(agora:*)
---

Show which peer sessions this one is linked to.

!`agora links`

If there are no links, suggest `/agora-link <peer-label>` to add one. If there are links, the user knows them now — no extra commentary needed unless they ask.
